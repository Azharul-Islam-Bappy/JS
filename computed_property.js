// Computed Property  

let propName = 'carName';

const Car = {
  [propName]: 'BMW', // here it is a  Computed Property because we are using the propName variable value as Property name using the brackets
  model: 'Mustang'
}
